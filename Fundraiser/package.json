{
  "name": "startup-funding-backend",
  "version": "1.0.0",
  "description": "Backend API for Startup Funding Portal",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup": "npm install && node completeDatabase.js",
    "test": "node -e \"console.log('Testing...'); fetch('http://localhost:5000/test').then(r=>r.json()).then(console.log).catch(console.error)\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "dotenv": "^16.3.1",
    "validator": "^13.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}